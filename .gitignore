zig-out/
zig-cache/
.zig-cache/
# dist/test-zig-package.sh's working dir; that script also asserts the
# fetched archive never contains it
zig-pkg/
*.o
*.so
*.a
core
core.*
*.swp
*.bak
.DS_Store
.claude/
external/

# per-user agent tool configs (contain local absolute paths)
.mcp.json
opencode.json

# makepkg working dirs (we keep dist/PKGBUILD; everything else is generated)
dist/src/
dist/pkg/
dist/sketerm/
dist/*.pkg.tar.*
dist/*.log

# dpkg packages built by dist/install.sh on Debian/Ubuntu
dist/*.deb

# Design documents are never committed (see CLAUDE.md): proposals stay
# untracked in the working tree. An agent committed the browser ones
# once; this makes that impossible to repeat by accident.
docs/proposal-*
